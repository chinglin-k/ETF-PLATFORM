name: Update ETF Data

on:
  schedule:
    # 每天 UTC 16:00（台灣時間隔日 00:00）執行，台股收盤資料通常此時已可取得
    - cron: '0 16 * * *'
  workflow_dispatch: {}   # 允許在 Actions 頁籤手動觸發，方便第一次部署時立即產生 data.json
  push:
    branches: [main]
    paths:
      - 'update_data.py'
      - 'requirements.txt'
      - '.github/workflows/update.yml'

permissions:
  contents: write   # 需要寫入權限才能 commit / push data.json

concurrency:
  group: update-etf-data
  cancel-in-progress: false

jobs:
  update-data:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run data update script
        run: python update_data.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data.json
          git diff --staged --quiet && echo "No changes to commit" || git commit -m "chore: auto-update ETF data $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          git push
